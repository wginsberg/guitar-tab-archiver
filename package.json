{
  "name": "guitar-tab-archiver",
  "displayName": "Guitar Tab Archiver",
  "version": "0.4.0",
  "description": "Archive tabs and chords",
  "author": "will",
  "scripts": {
    "dev": "plasmo dev",
    "build-clean": "rm -rf build/*.zip",
    "build-chrome": "plasmo build && zip -r build/chrome-v$npm_package_version.zip build/chrome-mv3-prod",
    "build-firefox": "plasmo build --target=firefox-mv2 && zip -r build/firefox-v$npm_package_version.zip build/firefox-mv2-prod",
    "build": "pnpm run build-clean && pnpm run build-chrome && pnpm run build-firefox",
    "package": "plasmo package"
  },
  "dependencies": {
    "@plasmohq/messaging": "^0.6.0",
    "classnames": "^2.3.2",
    "jszip": "^3.10.1",
    "plasmo": "0.84.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "4.1.1",
    "@types/chrome": "0.0.251",
    "@types/node": "20.9.0",
    "@types/react": "18.2.37",
    "@types/react-dom": "18.2.15",
    "prettier": "3.0.3",
    "typescript": "5.2.2"
  },
  "manifest": {
    "permissions": [
      "storage"
    ]
  }
}
